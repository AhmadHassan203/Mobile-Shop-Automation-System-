# Browser-visible configuration only. Never place database, session, migration,
# admin or seed credentials in a frontend env file.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api/v1
NEXT_PUBLIC_APP_NAME=MobileShop OS
